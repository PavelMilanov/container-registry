package handlers

import (
	"testing"
)

func TestUploadManifest(t *testing.T) {
	// imageName := "alpine"
	// reference := "latest"
	// manifest := config.NewManifest()

	// // Добавляем информацию о конфигурационном файле
	// if err := manifest.AddConfig("config.json"); err != nil {
	// 	log.Fatalf("couldn't add config: %v", err)
	// }

	// // Добавляем информацию о слое
	// if err := manifest.AddLayer("layer.tar.gz"); err != nil {
	// 	log.Fatalf("couldn't add layer: %v", err)
	// }

	// // Выводим манифест в формате JSON
	// if err := manifest.Print(); err != nil {
	// 	log.Fatalf("couldn't print manifest: %v", err)
	// }
}

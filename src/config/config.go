package config

var (
	DURATION = 5
	VERSION  string

	STORAGE_PATH  = "data"
	MANIFEST_PATH = "manifests"
	BLOBS_PATH    = "blobs"
	DATA_PATH     = "var"
	TMP_PATH      = "tmp"

	BACKET_NAME = "registry"
)
